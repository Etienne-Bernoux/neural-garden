{
  "name": "neural-garden-viewer",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "npx bddgen && npx playwright test",
    "test:all": "vitest run && npx bddgen && npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "playwright-bdd": "^8.5.0",
    "vitest": "^3.0.0"
  }
}
